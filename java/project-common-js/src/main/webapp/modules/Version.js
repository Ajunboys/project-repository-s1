var VvWork = VvWork || {};

/**
 * The unique ID of this version of the VvWork JavaScript API. This ID will
 * be the version string of the vvWork-common-js Maven project, and can be
 * used in downstream applications as a sanity check that the proper version
 * of the APIs is being used (in case an older version is cached, for example).
 *
 * @type {String}
 */
VvWork.API_VERSION = "0.9.9";
