<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Default appender -->
    <appender name="EASY_WORK-DEFAULT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Log at INFO level -->
    <root level="info">
        <appender-ref ref="EASY_WORK-DEFAULT" />
    </root>

</configuration>